import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "$nohouse — A roof for everyone",
  description:
    "$nohouse is a charity memecoin on Solana. 100% of trading rewards go to shelters and homelessness charities through donate.gg.",
  openGraph: {
    title: "$nohouse — A roof for everyone",
    description:
      "100% of $nohouse trading rewards fund shelters fighting homelessness, routed transparently through donate.gg.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-cream text-ink">
        {children}
      </body>
    </html>
  );
}
